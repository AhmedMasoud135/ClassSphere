// app/student/dashboard/loading.js
import Spinner from '@/app/components/Spinner';

export default function Loading() {
  // You can add any UI inside Loading, including a Skeleton.
  // For now, we'll use the same spinner.
  return <Spinner />;
}
